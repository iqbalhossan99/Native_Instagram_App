import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  lcContainer: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
    marginTop: "10px",
  },
  lcLeft: { display: "flex", flexDirection: "row", gap: "8px" },
  cmnText: {
    color: "#fff",
  },
});

export default styles;
