import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  story: {
    display: "flex",
    gap: "5px",
    alignItems: "center",
    marginRight: "20px",
  },
  storyName: {
    color: "#fff",
  },
});

export default styles;
